// console.log('This site was generated by Hugo.'); // Copy button for code blocks

// document.addEventListener("DOMContentLoaded", function () {
//     console.log("Script loaded successfully!"); // Check if the script runs

//     const codeBlocks = document.querySelectorAll("pre");
//     console.log("Found code blocks:", codeBlocks); // Check if code blocks are detected

//     codeBlocks.forEach((block) => {
//       const button = document.createElement("button");
//       button.innerHTML = `<i class="bi bi-clipboard"></i>`;
//       button.className = "copy-button";
//       block.style.position = "relative"; // Ensure proper button positioning
//       block.appendChild(button);

//       button.addEventListener("click", () => {
//         const codeElement = block.querySelector("code");
//         if (codeElement) {
//           const code = codeElement.innerText;
//           navigator.clipboard.writeText(code)
//             .then(() => {
//               console.log("Code copied to clipboard:", code);
//               button.innerHTML = `<i class="bi bi-clipboard2-check"></i>`;
//               setTimeout(() => {
//                 button.innerHTML = '<i class="bi bi-clipboard"></i>';
//               }, 2000);
//             })
//             .catch((err) => {
//               console.error("Failed to copy text:", err);
//             });
//         } else {
//           console.error("Code block not found inside <pre>.");
//         }
//       });
//     });
//   });



